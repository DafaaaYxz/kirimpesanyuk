<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Send Message</title>
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen p-6">
    <div class="w-full max-w-sm text-center">
        <div class="w-24 h-24 bg-blue-600 rounded-full mx-auto mb-6 flex items-center justify-center text-3xl font-black shadow-[0_0_30px_rgba(37,99,235,0.4)]">
            <%= target[0].toUpperCase() %>
        </div>
        <h2 class="text-xl font-bold mb-8">Kirim pesan rahasia ke @<%= target %></h2>
        <form action="/send/<%= target %>" method="POST">
            <textarea name="message" placeholder="Tulis pesanmu di sini..." class="w-full h-48 p-6 bg-[#0A0A0A] border border-[#1A1A1A] rounded-[2.5rem] focus:outline-none focus:border-blue-500 resize-none mb-6 text-lg" required></textarea>
            <button class="w-full bg-white text-black font-black py-5 rounded-full text-xl hover:scale-105 transition-transform">KIRIM!</button>
        </form>
    </div>
</body>
</html>
