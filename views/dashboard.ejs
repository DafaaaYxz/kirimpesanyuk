
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Dashboard</title>
</head>
<body class="bg-black text-white p-6">
    <div class="max-w-md mx-auto">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-2xl font-black italic">INBOX</h1>
            <a href="/logout" class="bg-red-600 text-[10px] font-bold px-3 py-1 rounded-full uppercase">Logout</a>
        </div>
        <div class="bg-red-600 p-4 rounded-2xl mb-6 shadow-lg shadow-red-900/20">
            <p class="text-[9px] font-bold uppercase mb-1 opacity-80">Link Kamu:</p>
            <p class="text-xs font-mono break-all font-bold">https://<%= host %>/u/<%= username %></p>
        </div>
        <div class="space-y-4">
            <% messages.forEach(msgStr => { const msg = JSON.parse(msgStr); %>
                <div class="bg-[#0a0a0a] border border-[#1a1a1a] p-5 rounded-2xl">
                    <div class="flex justify-between mb-2">
                        <span class="text-red-600 text-[9px] font-black uppercase">Anonim</span>
                        <span class="text-gray-600 text-[9px]"><%= msg.time %></span>
                    </div>
                    <p class="text-sm leading-relaxed text-gray-200"><%= msg.content %></p>
                </div>
            <% }) %>
            <% if(messages.length == 0) { %>
                <div class="text-center py-20 opacity-30 italic text-sm">Kosong...</div>
            <% } %>
        </div>
    </div>
</body>
</html>
